/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // IMPORTANT: don't set "output: 'export'" here,
  // that would disable your API routes.
};

export default nextConfig;
